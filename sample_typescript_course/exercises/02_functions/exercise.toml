[exercise]
name = "Functions"
order = 2
difficulty = "beginner"

[test]
timeout_seconds = 30

[hints]
enabled = true
hints = [
    "Arrow functions: const add = (a: number, b: number): number => a + b",
    "Optional parameters use ?: function greet(name?: string)",
    "Default parameters: function greet(name: string = 'World')"
]
