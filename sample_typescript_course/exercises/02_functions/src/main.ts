// TODO: Implement typed functions

export function multiply(a: number, b: number): number {
  // TODO: Return a * b
  return 0;
}

export function greet(name?: string): string {
  // TODO: Return "Hello, {name}!" or "Hello, stranger!" if name is undefined
  return "";
}

export function repeat(str: string, times: number = 1): string {
  // TODO: Return str repeated times times
  return "";
}

export function createCounter(): () => number {
  // TODO: Return a closure that increments and returns a counter
  return () => 0;
}
