[exercise]
name = "Interfaces"
order = 3
difficulty = "intermediate"

[test]
timeout_seconds = 30

[hints]
enabled = true
hints = [
    "Define interfaces with: interface Person { name: string; age: number; }",
    "Optional properties use ?: interface Person { nickname?: string; }",
    "Readonly properties: interface Point { readonly x: number; }"
]
