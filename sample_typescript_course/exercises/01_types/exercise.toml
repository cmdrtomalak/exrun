[exercise]
name = "Basic Types"
order = 1
difficulty = "beginner"

[test]
timeout_seconds = 30

[hints]
enabled = true
hints = [
    "TypeScript has basic types: string, number, boolean",
    "Use : Type after variable names to annotate types",
    "Arrays can be typed as number[] or Array<number>"
]
