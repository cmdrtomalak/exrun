// TODO: Implement the functions with proper type annotations

export function formatGreeting(name: string, age: number): string {
  // TODO: Return "Hello, {name}! You are {age} years old."
  return "";
}

export function sumArray(numbers: number[]): number {
  // TODO: Return the sum of all numbers
  return 0;
}

export function isEven(n: number): boolean {
  // TODO: Return true if n is even
  return false;
}

export function getFirstAndLast<T>(arr: T[]): [T, T] {
  // TODO: Return a tuple with first and last elements
  return [arr[0], arr[0]];
}
