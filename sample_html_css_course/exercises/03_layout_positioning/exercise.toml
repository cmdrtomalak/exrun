[exercise]
name = "Layout & Positioning"
order = 3
difficulty = "intermediate"

[test]
timeout_seconds = 30

[hints]
enabled = true
hints = [
    "Use Flexbox for one-dimensional layouts (navigation, card arrangements)",
    "Use CSS Grid for two-dimensional layouts (overall page layout)",
    "Apply position properties for overlay elements or fixed navigation",
    "Use display: flex, grid-template-columns, grid-gap for layout control"
]
