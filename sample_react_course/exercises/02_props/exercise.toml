[exercise]
name = "Props"
order = 2
difficulty = "beginner"

[test]
timeout_seconds = 60

[hints]
enabled = true
hints = [
    "Props are passed to components like HTML attributes: <Greeting name='Alice' />",
    "Access props as the first parameter: function Greeting({ name }) { ... }",
    "Use TypeScript interfaces to define prop types"
]
