// TODO: Create a UserCard component that accepts name and age props
//
// Requirements:
// 1. Define a UserCardProps interface with name (string) and age (number)
// 2. Export a function component named UserCard
// 3. Return a div with className="user-card"
// 4. Inside the div, render an h2 with the name
// 5. Also render a p with "Age: {age}"

interface UserCardProps {
  // TODO: Define the props
}

export function UserCard(props: UserCardProps) {
  // TODO: Return the JSX
  return null;
}
