[exercise]
name = "State"
order = 3
difficulty = "intermediate"

[test]
timeout_seconds = 60

[hints]
enabled = true
hints = [
    "Import useState from 'react': import { useState } from 'react'",
    "useState returns an array: const [count, setCount] = useState(0)",
    "Update state with the setter function: setCount(count + 1)",
    "Attach onClick handlers to buttons: <button onClick={handleClick}>"
]
