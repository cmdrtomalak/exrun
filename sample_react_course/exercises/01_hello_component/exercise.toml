[exercise]
name = "Hello Component"
order = 1
difficulty = "beginner"

[test]
timeout_seconds = 60

[hints]
enabled = true
hints = [
    "A React component is a function that returns JSX",
    "JSX looks like HTML but uses className instead of class",
    "Export your component with 'export function' or 'export default'"
]
