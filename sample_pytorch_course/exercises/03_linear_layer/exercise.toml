[exercise]
name = "Linear Layer"
order = 3
difficulty = "intermediate"

[test]
timeout_seconds = 30

[hints]
enabled = true
hints = [
    "torch.nn.Linear(in_features, out_features) creates a linear layer",
    "A linear layer computes: output = input @ weight.T + bias",
    "Check the .weight and .bias attributes of the Linear module"
]
