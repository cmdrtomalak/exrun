[exercise]
name = "Simple Neural Network"
order = 4
difficulty = "intermediate"

[test]
timeout_seconds = 60

[hints]
enabled = true
hints = [
    "Inherit from nn.Module and call super().__init__() in __init__",
    "Define layers as attributes in __init__: self.fc1 = nn.Linear(...)",
    "In forward(), pass input through layers with activation functions",
    "Use torch.relu() or nn.functional.relu() for ReLU activation"
]
